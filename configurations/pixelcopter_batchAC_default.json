{
  "agent_config": {
    "name": "pixelcopter_batchAC_default_02",
    "desc": "Default configuration to train a batch actor critic agent on Pixelcopter",
    "env": "pixelcopter",
    "agent_type": "batch_AC",
    "discount": 0.99
  },
  "critic_config": {
    "layer_sizes": [256, 128],
    "learning_rate": 0.0001,
    "hidden_activation": "relu",
    "output_activation": "linear"
  },
  "actor_config": {
    "layer_sizes": [256, 128],
    "learning_rate": 0.0001,
    "hidden_activation": "relu"
  },
  "training_config": {
    "train_steps": 40000,
    "save_policy_every": 200,
    "show_every": 500
  }
}